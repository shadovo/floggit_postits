'use strict';

describe('Directive: fpCustomFunctionality', function () {

  // load the directive's module
  // beforeEach(module('floggitPostitsApp'));

  // var element,
  //   scope;

  // beforeEach(inject(function ($rootScope) {
  //   scope = $rootScope.$new();
  // }));

  // it('should make hidden element visible', inject(function ($compile) {
  //   element = angular.element('<fp-custom-functionality></fp-custom-functionality>');
  //   element = $compile(element)(scope);
  //   expect(element.text()).toBe('this is the fpCustomFunctionality directive');
  // }));
});
